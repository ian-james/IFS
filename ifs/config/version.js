// this version number should stay in sync with the number reported by
// `npm version`
/*

 var version = "0.0.3";

// show the version number at the footer of every page
var vstr = "IFS v" + version;
window.onload = function() {
    document.getElementById("IFS-version").innerHTML = vstr;
}
*/
