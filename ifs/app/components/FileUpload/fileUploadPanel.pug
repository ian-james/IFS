div.panel.panel-primary
    div.panel-heading
        h4.panel-title.text-center
            p File Upload
    div(class="panel-body text-center" uploader="uploader" ng-controller="fileUploadCtrl" filters="queueLimit, customFilter")
        h4 Please select files to upload
        hr
        input(type="file" name="file" multiple required)
        input(type="hidden" name="time"  id="time" value=Date.now())
        hr
        //include fileUploadFileDisplay.pug
        hr
        div
        input(type="submit" value="Evaluate" name="submit")

    script(src="components/FileUpload/fileUploadController.js")