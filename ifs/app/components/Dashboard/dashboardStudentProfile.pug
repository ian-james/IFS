div
    div(class="uk-width-1-1").focusCard
           include dashboardSkillFocus.pug
    h2 Your Profile
    div(uk-grid).uk-grid.uk-grid-small
        div(class="uk-width-1-1").skills
            div.uk-card.uk-card-default
                div.uk-card-body
                    h2.uk-card-title Skills
                    p.uk-text-meta Keep track of course progress by self-rating skills. This data is purely formative.
                    div(ng-if="skills && skills.length > 0")
                        div(ng-repeat="item in skills|limitTo:3")
                            p {{item.skillName}}
                            progress(value="{{item.value*100}}" max="100" title="{{item.value*100 + '%'}}", uk-tooltip).uk-progress
                    div(ng-if="!skills || skills.length == 0")
                        p You haven't rated any of your skills yet! Rating your skills will help you keep track of how you feel about your coursework.
                    a(href="/skills" class="uk-width-1-1 uk-width-auto@s").uk-button.uk-button-default
                        | View More Skills
                        span(uk-icon="icon: info").uk-margin-small-left
        div(class="uk-width-1-1@s uk-width-1-3@m").classes
            div.uk-card.uk-card-default
                div.uk-card-body
                    h2.uk-card-title Classes
                    div(ng-if="courses")
                        p.uk-text-meta You are currently enrolled in:
                        dl.uk-description-list.uk-description-list-divider
                            dt(ng-repeat="val in courses")
                                strong {{val.code + ": " + val.courseName}}
                    div(ng-if="courses.length===0")
                        p
                            | You haven't enrolled in a class yet. Visit 
                            a(href="/setup") setup 
                            | to enroll.
        div(class="uk-width-1-1@1 uk-width-2-3@m").models
            div.uk-card.uk-card-default
                div.uk-card-body
                    h2.uk-card-title Statistics
                    p.uk-text-meta View various metrics about your own performance and your class.
                    div(class="uk-child-width-1-1 uk-child-width-1-2@s uk-child-width-1-3@l")
                        a(href="/studentModel").uk-button.uk-button-default View Your Stats 
                            span(uk-icon="icon: info").uk-margin-small-left
                        a(href="/socialModel").uk-button.uk-button-default View Class Stats 
                             span(uk-icon="icon: info").uk-margin-small-left