h3 Generalized Feedback
table.uk-table
    if feedbackItems && feedbackItems.length != 0
        thead
            tr
                th Tool Name
                th Type
                th Feedback
                th File
                th Line
                th Character
                th Target
            tbody
                each f in feedbackItems
                    if f.filename === ""
                        tr 
                            td #{f.toolName}
                            td #{f.type}
                            td #{f.feedback}
                            td ""
                            td ""
                            td ""
                            td #{f.target}
            tbody
                 each f in feedbackItems
                    if f.filename !== ""
                        tr 
                            td #{f.toolName}
                            td #{f.type}
                            td #{f.feedback}
                            td #{f.filename}
                            td #{f.lineNum}
                            td #{f.charPos}
                            td #{f.target}
    else
        tbody
            tr
                td Congratulations, no errors were found!
